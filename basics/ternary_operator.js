const userName = isLogin ? 'Max' : null

/*
isLogin Codicao para checar, incluindo multiplas condicoes como and e or
'Max' Valor se True
null Valor se False

pode ter possuir nested tenary, ex:

const userName = isLogin ? IsAdmin ? 'all': 'nothing': null

*/