//console.dir(document); // expondo todas as propriedade de um objeto

//window.alert('teste'); 
// alert é uma função do browser, se a função nao tiver nada na frente, é pega do window.<METODO>
// ex: window.document

//querySelector(), getElementById() // ou querySelectorAll(); getXByY => list of nodes
//Return single elements

//nodes = object that make up the DOM
//Elements = One type of nodes, with methods like 
